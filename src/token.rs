#[derive(Debug, Clone, PartialEq)]
pub enum TokenKind {
    Module,
    Use,
    Pub,
    Pack,
    Func,
    Let,
    Mut,
    Return,
    If,
    Else,
    For,
    In,
    By,
    While,
    Loop,
    Break,
    Skip,
    Ask,
    True,
    False,
    Identifier(String),
    Integer(i64),
    String(String),
    LeftParen,
    RightParen,
    LeftBrace,
    RightBrace,
    LeftBracket,
    RightBracket,
    Comma,
    Semicolon,
    Dot,
    DotDot,
    ColonColon,
    Plus,
    Minus,
    Star,
    Slash,
    Percent,
    Equal,
    EqualEqual,
    Bang,
    BangEqual,
    Less,
    LessEqual,
    Greater,
    GreaterEqual,
    AmpAmp,
    PipePipe,
    Eof,
}

#[derive(Debug, Clone, PartialEq)]
pub struct Token {
    pub kind: TokenKind,
    pub line: usize,
    pub column: usize,
}
